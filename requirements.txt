# Runtime dependencies for DEEPSecurity.
#
# Tested against Python 3.11 and 3.12.
# Python 3.14 works for every core dep below; the OPTIONAL ML extras
# (scikit-learn, numpy) don't yet have Windows wheels for 3.14 — that's
# why they're extras, not core.
#
# Install:
#     pip install -r requirements.txt            # core — pure-Python
#     pip install "deepsecurity[ml]"              # + sklearn + numpy
#     pip install "deepsecurity[watchdog]"        # + real-time file monitor
#     pip install "deepsecurity[yara]"            # + YARA rule engine
#     pip install "deepsecurity[windows]"         # + Outlook support

# --- Web framework -------------------------------------------------------
Flask>=3.0,<4.0
flask-cors>=5.0
flask-jwt-extended>=4.7
gunicorn>=23.0

# --- Data + persistence --------------------------------------------------
SQLAlchemy>=2.0,<3.0

# --- Config + validation -------------------------------------------------
pydantic>=2.11,<3.0
pydantic-settings>=2.6,<3.0
python-dotenv>=1.0

# --- Utilities -----------------------------------------------------------
joblib>=1.3
psutil>=6.0
structlog>=24.0
click>=8.1
