// DEPRECATED — replaced by ScanPanel.jsx + Dashboard.jsx.
// Kept only because the sandbox that produced this refactor could not
// delete files it did not own. Safe to `rm` by hand.
export default function ScanDashboard() {
  return null
}
